{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "A scalable URL shortening service with MongoDB sharding and Redis caching",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest",
    "lint": "eslint src/",
    "setup": "node setup.js",
    "docker:build": "docker build -t url-shortener .",
    "docker:run": "docker run -p 3000:3000 --env-file .env url-shortener"
  },
  "keywords": ["url-shortener", "nodejs", "express", "mongodb", "redis"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "redis": "^4.6.8",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "express-rate-limit": "^6.10.0",
    "express-validator": "^7.0.1",
    "dotenv": "^16.3.1",
    "nanoid": "^3.3.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.6.2",
    "eslint": "^8.47.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 